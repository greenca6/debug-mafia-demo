version: '3.3'

services:
  api:
    image: demo-api
    build:
      context: ./api
      dockerfile: Dockerfile.deps
    ports:
      - 8080:8080
  
  ui:
    image: demo-ui
    build: 
      context: ./ui
      dockerfile: Dockerfile.deps
    ports:
      - 3000:3000
